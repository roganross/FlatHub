import React from 'react';
import { View, Text } from 'react-native';

const TodoScreen = () => {
  return (
    <View>
      <Text>Todo Screen</Text>
    </View>
  );
};

export default TodoScreen; // Make sure this line exists
